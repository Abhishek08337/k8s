apiVersion: v1
kind: Service 
metadata:
  name: tomcatcip
  labels:
    type: tccip 
spec:
  selector:
    app: tomcatpod 
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  