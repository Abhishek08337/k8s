apiVersion: apps/v1
kind: StatefulSet 
metadata:
  name: sset 
  labels:
    app: ssetdb
spec:
  replicas: 3
  selctor:
    matchLabels:
      app: mysqlsset
  template:
    metadata:
      name: mysqlpod
      lables:
        app: mysqlsset
    spec:
      containers:
        - name: mysqlcontainer
          image: mysql:latest
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: MYSQL_ROOT_PASSWORD
              secretKeyRef:
                - name: mysqlsecretkey
                  key: my_pass
